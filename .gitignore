docs/
.claude
system-tester/autoloop

# autoloop evidence (large; keep the skill itself)
software-engineer/.autoloop/iters/
software-engineer/.autoloop/snapshots/

# python bytecode from skill scripts
__pycache__/
*.pyc

# virtualenv symlinked from AiAgents/speaker
.venv
node_modules
# secrets: API keys live outside the repo (~/.config/create-image-gemini/). Never commit them.
.env
*.env
!*.env.example
create-image-gemini/**/config.json
*gemini*key*
generated-images/
